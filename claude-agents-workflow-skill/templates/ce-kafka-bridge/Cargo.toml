[package]
name = "ce-kafka-bridge"
version = "0.1.0"
edition = "2021"

[dependencies]
tiny_http = "0.12"
rdkafka = { version = "0.36", default-features = false, features = ["cmake-build"] }
serde_json = "1"

[profile.release]
strip = true
opt-level = "z"
lto = true
