# ============================================
# Development Environment Docker Compose
# {{PROJECT_NAME}}
# ============================================

services:
  # === Application Service ===
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: {{PROJECT_NAME}}-app-dev
    restart: unless-stopped
    ports:
      - "{{APP_PORT}}:{{APP_PORT}}"
    volumes:
      - .:/app
      - /app/node_modules  # Exclude node_modules if Node.js
    environment:
      - NODE_ENV=development
      - SPRING_PROFILES_ACTIVE=dev
      - FLASK_ENV=development
      - DATABASE_URL=postgresql://app:app@db:5432/{{PROJECT_NAME}}_dev
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:{{APP_PORT}}/health"]
      interval: 30s
      timeout: 5s
      retries: 3
    networks:
      - app-net

  # === Database ===
  db:
    image: postgres:18-alpine
    container_name: {{PROJECT_NAME}}-db-dev
    restart: unless-stopped
    ports:
      - "5432:5432"
    volumes:
      # postgres:18 起 PGDATA 是 /var/lib/postgresql/18/docker,VOLUME 是
      # /var/lib/postgresql。掛在舊的 .../data 上會被 entrypoint 拒絕啟動(exit 1)。
      # 這是給全新部署用的模板;既有的 16/17 叢集不能只改這兩行,需要 pg_dumpall 遷移。
      - db_data_dev:/var/lib/postgresql
    environment:
      - POSTGRES_USER=app
      - POSTGRES_PASSWORD=app
      - POSTGRES_DB={{PROJECT_NAME}}_dev
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U app"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - app-net

  # === Redis (Optional) ===
  # redis:
  #   image: redis:7-alpine
  #   container_name: {{PROJECT_NAME}}-redis-dev
  #   ports:
  #     - "6379:6379"
  #   networks:
  #     - app-net

volumes:
  db_data_dev:

networks:
  app-net:
    driver: bridge
